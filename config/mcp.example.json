{
  "_comment": "Claude Code MCP config for capital-mcp. Copy to config/mcp.json and adjust the command/args to match your install method (docker-exec, uvx, or local venv). CAP_* env is inherited from the calling shell; do not embed secrets here.",
  "mcpServers": {
    "capital-com": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "capital-mcp",
        "python",
        "-m",
        "capital_mcp"
      ]
    }
  }
}
