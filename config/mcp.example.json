{
  "_comment": "MCP config consumed by `claude -p --mcp-config config/mcp.json`. On Windows, keep .venv\\Scripts\\python.exe; on Linux/macOS, .venv/bin/python. CAP_* is read from the calling shell's environment (populated from .env by our launcher). Never embed secrets here.",
  "mcpServers": {
    "capital-com": {
      "command": ".venv\\Scripts\\python.exe",
      "args": ["-m", "capital_mcp"]
    }
  }
}
